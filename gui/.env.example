# Copy this file to .env and edit the paths

# Where to store downloaded AAXC files
DOWNLOAD_PATH=/mnt/audiobooks/downloads

# Where to store converted M4B files
CONVERTED_PATH=/mnt/audiobooks/converted

# Where to move source files after conversion (optional)
COMPLETED_PATH=/mnt/audiobooks/completed

# Your timezone (for correct timestamps)
TZ=America/Los_Angeles

# Port to access the web UI (default: 8501)
PORT=8501
